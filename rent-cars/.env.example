VITE_API_URL=http://localhost:5555

# Set to false when the backend runs on Vercel (or any serverless host) — it
# can't hold Socket.io connections open, so the client shouldn't try.
VITE_ENABLE_REALTIME=true
